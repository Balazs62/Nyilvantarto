<Window x:Class="Nyilvantarto.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Nyilvantarto"
        mc:Ignorable="d"
        Title="Nyilvantarto" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox" Grid.Column="0" 
              VerticalContentAlignment="Center" FontSize="14"
              Tag="Keresés..." Text=""/>

            <Button Grid.Column="1" Content="Keresés" Margin="5,0,0,0" Padding="15,5" Click="SearchButton_Click"/>
            
            <Button Grid.Column="2" Content="Kiadás" Margin="5,0,0,0" Padding="15,5" Click="StockOutButton_Click" />

            <Button Grid.Column="4" Content="+ Új anyag felvétele" 
             Padding="15,5" Background="#007ACC" Foreground="White" FontWeight="Bold"
             Click="AddButton_Click"/>
        </Grid>

        <DataGrid x:Name="MaterialsGrid" Grid.Row="1" 
           AutoGenerateColumns="False" 
           CanUserAddRows="False" 
           IsReadOnly="True"
           HeadersVisibility="Column"
           GridLinesVisibility="Horizontal">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Megnevezés" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Cikkszám" Binding="{Binding Id}" Width="100" />
                <DataGridTextColumn Header="Mennyiség" Binding="{Binding Quantity}" Width="100" />
                <DataGridTextColumn Header="Egység" Binding="{Binding Unit}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
